#include <stdio.h>

int main()
{
	int n = 0;
	char current; before;
	scanf("%d", &n);

	for (int i = 0; i < n; ++i)
	{
		scanf("%c", &current);
		if ( i == 0)
		{
			before = current;	
		}
	}


	return 0;
}


////// NOT FINISHED